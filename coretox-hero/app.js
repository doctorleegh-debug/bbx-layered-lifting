(()=>{'use strict';const v=document.querySelector('video'),reduced=matchMedia('(prefers-reduced-motion: reduce)');let visible=true;function sync(){if(document.hidden||reduced.matches||!visible){v.pause();return}v.muted=true;const p=v.play();if(p)p.catch(()=>{});}new IntersectionObserver(entries=>{visible=entries[0].isIntersecting;sync()},{threshold:.1}).observe(v);document.addEventListener('visibilitychange',sync);reduced.addEventListener('change',sync);if(reduced.matches)v.removeAttribute('autoplay');sync();})();
